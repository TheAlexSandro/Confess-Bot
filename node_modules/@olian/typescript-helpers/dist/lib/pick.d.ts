import { ExpandType } from './ExpandType';
/**
 * Create subsets of objects in a type-safe way
 *
 * ```ts
 * const target = {
 *    a: 1,
 *    b: 'A',
 *    c: [1, 2, 3],
 * };
 *
 * const bar = pick(target, 'a', 'c');
 * typeof bar /* {
 *    a: number;
 *    c: number[];
 * }
 * ```
 */
export declare const pick: <T extends object, K extends keyof T>(obj: T, ...include: K[]) => ExpandType<Pick<T, K>>;
/**
 * Create subsets of objects in a value-safe & type-safe way
 *
 * ```ts
 * import { someObject } from '...';
 * const fallback = {
 *    a: 1,
 *    b: 'A',
 *    c: [1, 2, 3],
 * };
 * const bar = safePick(someObject, fallback, 'a', 'c');
 * typeof bar /* === {
 *    a: number;
 *    c: number[];
 * }
 * ```
 */
export declare const safePick: <T extends object, K extends keyof T>(obj: Partial<T>, fallback: T, ...include: K[]) => ExpandType<Pick<T, K>>;
